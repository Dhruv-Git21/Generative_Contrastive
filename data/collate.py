# (Optional separate collate implementations if needed. In this implementation, we used a collate method within EmbeddingDataset.)
# If different models required special collation, they could be implemented here.
# For this codebase, we use the dataset's own collate_fn for simplicity.
